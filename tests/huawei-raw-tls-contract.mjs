import assert from "node:assert/strict";
const {probeHuaweiRawTls}=await import("../src/huawei-raw-tls-canary.js");
const result=await probeHuaweiRawTls({});
assert.equal(result.ok,false);
assert.equal(result.configured,false);
assert.equal(result.transport,"node:tls-manual-http1");
assert.equal(result.wire_shape,"official-listfunctions-empty-get");
assert.equal(result.body_bytes,0);
assert.equal(result.http_status,0);
assert.equal(result.upstream_called,false);
assert.equal(result.route_eligible,false);
assert.equal(result.paid_fallback,false);
assert.equal(result.secret_echo,false);
console.log(JSON.stringify({ok:true,suite:"huawei-raw-tls-contract",node_tls_imported:true,manual_http1:true,no_upstream:true,secrets_redacted:true}));
