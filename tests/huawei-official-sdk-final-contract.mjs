import assert from "node:assert/strict";
const {probeHuaweiOfficialSdkFinal}=await import("../src/huawei-official-sdk-final-canary.js");
const result=await probeHuaweiOfficialSdkFinal({});
assert.equal(result.ok,false);
assert.equal(result.configured,false);
assert.equal(result.official_sdk_used,true);
assert.equal(result.official_sdk_version,"3.1.209");
assert.equal(result.http_status,0);
assert.equal(result.upstream_called,false);
assert.equal(result.route_eligible,false);
assert.equal(result.paid_fallback,false);
assert.equal(result.secret_echo,false);
console.log(JSON.stringify({ok:true,suite:"huawei-official-sdk-final-contract",official_sdk_imported:true,official_sdk_version:"3.1.209",no_upstream:true,secrets_redacted:true}));
