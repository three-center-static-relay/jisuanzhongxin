import assert from "node:assert/strict";
const {probeHuaweiNodeHttpsGetBody}=await import("../src/huawei-node-http-get-body-canary.js");
const result=await probeHuaweiNodeHttpsGetBody({});
assert.equal(result.ok,false);
assert.equal(result.configured,false);
assert.equal(result.transport,"node:https");
assert.equal(result.request_semantics,"get-with-json-object-body");
assert.equal(result.body_bytes,2);
assert.equal(result.http_status,0);
assert.equal(result.upstream_called,false);
assert.equal(result.route_eligible,false);
assert.equal(result.paid_fallback,false);
assert.equal(result.secret_echo,false);
console.log(JSON.stringify({ok:true,suite:"huawei-node-http-get-body-contract",node_https_imported:true,no_upstream:true,secrets_redacted:true}));
