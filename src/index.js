
const API_VERSION = "2026-08-14";
const MAX_BODY_BYTES = 65536;
const DEFAULT_LEASE_SECONDS = 1800;
const MAX_LEASE_SECONDS = 7200;
const DEFAULT_RATE_PER_MIN = 60;
function now(){ return new Date().toISOString(); }
function rid(){ return crypto.randomUUID(); }
function int(v,d){ const n=Number(v); return Number.isFinite(n)?Math.trunc(n):d; }
function json(body,status=200,headers={}){ return Response.json(body,{status,headers:{"cache-control":"no-store",...headers}}); }
function error(code,message,status=400,details){ return json({ok:false,error:code,message,details},status); }
async function parseJson(req){ const len=Number(req.headers.get("content-length")||0); if(len>MAX_BODY_BYTES) throw Object.assign(new Error("BODY_TOO_LARGE"),{status:413}); const t=await req.text(); if(new TextEncoder().encode(t).length>MAX_BODY_BYTES) throw Object.assign(new Error("BODY_TOO_LARGE"),{status:413}); if(!t) return {}; try{return JSON.parse(t);}catch{throw Object.assign(new Error("INVALID_REQUEST"),{status:400});} }
function redact(v){ if(Array.isArray(v)) return v.map(redact); if(v&&typeof v==="object"){ const o={}; for(const [k,x] of Object.entries(v)) o[k]=/token|secret|password|authorization|cookie|api.?key/i.test(k)?"[REDACTED]":redact(x); return o;} return v; }
async function sha256Text(text){ const data=new TextEncoder().encode(text); const hash=await crypto.subtle.digest("SHA-256",data); return [...new Uint8Array(hash)].map(x=>x.toString(16).padStart(2,"0")).join(""); }
async function sourceDigest(service,policy,capabilities){ return sha256Text(JSON.stringify({service,api_version:API_VERSION,policy,capabilities})); }
function gate(env){ const id=env.CENTER_GATE.idFromName("global"); return env.CENTER_GATE.get(id); }
async function gateCall(env,path,method="GET",body){ const init={method,headers:{"content-type":"application/json"}}; if(body!==undefined) init.body=JSON.stringify(body); const r=await gate(env).fetch(new Request(`https://gate.internal${path}`,init)); const b=await r.json().catch(()=>({ok:false,error:"GATE_BAD_RESPONSE"})); return {http:r.status,...b}; }
async function acquire(env,taskId,kind,leaseSeconds){ return gateCall(env,"/acquire","POST",{task_id:taskId,kind,lease_seconds:leaseSeconds}); }
async function release(env,taskId){ return gateCall(env,"/release","POST",{task_id:taskId}); }
async function saveTask(env,taskId,patch){ return gateCall(env,`/task/${encodeURIComponent(taskId)}`,"POST",patch); }
async function loadTask(env,taskId){ return gateCall(env,`/task/${encodeURIComponent(taskId)}`); }
async function checkRate(env){ return gateCall(env,"/rate","POST",{limit:int(env.RATE_LIMIT_PER_MIN,DEFAULT_RATE_PER_MIN)}); }
export class CenterGate { constructor(state,env){ this.state=state; this.env=env; } async fetch(req){ const u=new URL(req.url), s=this.state.storage; const body=async()=>{try{return await req.json();}catch{return {};}}; const active=async()=>{ const a=await s.get("active"); if(a && a.expires_at_ms<=Date.now()){ await s.delete("active"); return null;} return a||null;}; if(req.method==="GET"&&u.pathname==="/state") return json({ok:true,active:await active()}); if(req.method==="POST"&&u.pathname==="/acquire"){ const b=await body(),a=await active(); if(a&&a.task_id!==b.task_id) return json({ok:false,error:"BUSY",active:a},409); const lease=Math.max(30,Math.min(MAX_LEASE_SECONDS,int(b.lease_seconds,DEFAULT_LEASE_SECONDS))); const rec={task_id:String(b.task_id||""),kind:String(b.kind||"task"),acquired_at:now(),expires_at_ms:Date.now()+lease*1000}; if(!rec.task_id) return json({ok:false,error:"INVALID_REQUEST"},400); await s.put("active",rec); return json({ok:true,active:rec}); } if(req.method==="POST"&&u.pathname==="/release"){ const b=await body(),a=await active(); if(!a) return json({ok:true,released:false}); if(a.task_id!==b.task_id) return json({ok:false,error:"LOCK_OWNER_MISMATCH",active:a},409); await s.delete("active"); return json({ok:true,released:true}); } if(req.method==="POST"&&u.pathname==="/rate"){ const b=await body(),limit=Math.max(1,Math.min(1000,int(b.limit,DEFAULT_RATE_PER_MIN))); const minute=Math.floor(Date.now()/60000),key=`rate:${minute}`; const count=(await s.get(key)||0)+1; await s.put(key,count); await s.delete(`rate:${minute-2}`); return count<=limit?json({ok:true,count,limit}):json({ok:false,error:"RATE_LIMITED",count,limit},429);} let m=u.pathname.match(/^\/task\/([^/]+)$/); if(m&&req.method==="GET") return json({ok:true,task:await s.get(`task:${decodeURIComponent(m[1])}`)||null}); if(m&&req.method==="POST"){ const id=decodeURIComponent(m[1]),b=await body(),old=await s.get(`task:${id}`)||{}; const rec={...old,...redact(b),task_id:id}; await s.put(`task:${id}`,rec); return json({ok:true,task:rec}); } m=u.pathname.match(/^\/task\/([^/]+)\/cancel$/); if(m&&req.method==="POST"){ const id=decodeURIComponent(m[1]),key=`task:${id}`,old=await s.get(key); if(!old) return json({ok:false,error:"TASK_NOT_FOUND"},404); const rec={...old,cancel_requested:true,cancel_requested_at:now()}; await s.put(key,rec); return json({ok:true,task:rec}); } return json({ok:false,error:"GATE_ROUTE_NOT_FOUND"},404); } }
const SERVICE="compute-worker";
const POLICY={fail_closed:true,single_active_task:true,cloudflare_heavy_compute:false,executor:"kaggle",arbitrary_code_upload:false,arbitrary_shell:false,max_retries:1,timeout_cleanup:true,duplicate_task_rejected:true};
const CAPABILITIES={router:true,durable_gate:true,kaggle_official_mcp:true,cpu:true,gpu_t4:true,profiles:["core","bayesian","simulation","causal","finance","gis","gpu","optimization"],heavy_suites:["kaggle_cpu","kaggle_t4","cooperation_chaos_100k","routing_budget_140k"]};
const OPENAPI='{"openapi":"3.1.0","info":{"title":"Compute Center","version":"2026-08-14"},"paths":{"/health":{"get":{}},"/v1/policy":{"get":{}},"/v1/capabilities":{"get":{}},"/v1/run":{"post":{}},"/v1/status":{"post":{}},"/v1/cancel":{"post":{}}}}';
function readiness(env){ return {provider:"kaggle",remote_mcp:"https://www.kaggle.com/mcp",bridge_configured:Boolean(env.KAGGLE_MCP_BRIDGE_URL),auth_configured:Boolean(env.KAGGLE_API_TOKEN||env.KAGGLE_MCP_AUTH)}; }
async function handle(req,env,ctx){ const u=new URL(req.url); if(req.method==="GET"&&u.pathname==="/health") return json({ok:true,status:"ready",service:SERVICE,api_version:API_VERSION,source:"github-static-relay",kaggle:readiness(env)}); if(req.method==="GET"&&(u.pathname==="/v1/policy"||u.pathname==="/policy")) return json({ok:true,service:SERVICE,policy:POLICY}); if(req.method==="GET"&&(u.pathname==="/v1/capabilities"||u.pathname==="/capabilities")) return json({ok:true,service:SERVICE,capabilities:CAPABILITIES,kaggle:readiness(env)}); if(req.method==="GET"&&(u.pathname==="/quota"||u.pathname==="/v1/quota")) return json({ok:true,rate_limit_per_min:int(env.RATE_LIMIT_PER_MIN,30),single_active_task:true,max_runtime_seconds:int(env.MAX_RUNTIME_SECONDS,3600),max_retries:1}); if(req.method==="GET"&&u.pathname==="/openapi.json") return new Response(OPENAPI,{headers:{"content-type":"application/json","cache-control":"no-store"}}); if(req.method==="GET"&&u.pathname==="/source") return json({ok:true,service:SERVICE,api_version:API_VERSION,source_digest:await sourceDigest(SERVICE,POLICY,CAPABILITIES),secrets_redacted:true}); if(req.method==="GET"&&u.pathname==="/v1/acceptance/latest") return json({ok:true,service:SERVICE,status:"not_verified",run_id:null,receipt_digest:null}); if(req.method==="POST"&&u.pathname==="/v1/status"){ const b=await parseJson(req); if(!b.task_id) return error("INVALID_REQUEST","task_id required",400); const local=await loadTask(env,b.task_id); if(!local.task) return error("INVALID_REQUEST","Task not found",404); if(local.task.remote_status_url){ try{ const r=await fetch(local.task.remote_status_url,{headers:env.KAGGLE_MCP_AUTH?{"authorization":`Bearer ${env.KAGGLE_MCP_AUTH}`}:{}}); const rb=await r.json().catch(()=>null); return json({ok:r.ok,task:local.task,upstream:redact(rb),upstream_status:r.status},r.ok?200:502);}catch(e){ return error("UPSTREAM_UNAVAILABLE","Kaggle bridge status failed",502,{task:local.task}); }} return json({ok:true,task:local.task}); } if(req.method==="POST"&&u.pathname==="/v1/cancel"){ const b=await parseJson(req); if(!b.task_id) return error("INVALID_REQUEST","task_id required",400); const t=await loadTask(env,b.task_id); if(t.task?.remote_cancel_url){ try{await fetch(t.task.remote_cancel_url,{method:"POST",headers:env.KAGGLE_MCP_AUTH?{"authorization":`Bearer ${env.KAGGLE_MCP_AUTH}`}:{}})}catch{} } const r=await gateCall(env,`/task/${encodeURIComponent(b.task_id)}/cancel`,"POST",{}); await release(env,b.task_id); return json({ok:r.ok,task:r.task||null}); } if(req.method==="POST"&&u.pathname==="/v1/run"){ const rate=await checkRate(env); if(!rate.ok) return error("RATE_LIMITED","Compute budget exceeded",429,rate); const b=await parseJson(req),taskId=String(b.task_id||b.request_id||rid()); const profile=String(b.profile||"core"); if(!CAPABILITIES.profiles.includes(profile)) return error("INVALID_REQUEST","Unknown compute profile",400,{allowed:CAPABILITIES.profiles}); const acq=await acquire(env,taskId,"compute",Math.min(MAX_LEASE_SECONDS,int(b.timeout_seconds,1800))); if(!acq.ok) return error("BUSY","Another compute task is active",409,acq.active); const rd=readiness(env); await saveTask(env,taskId,{status:"accepted",profile,created_at:now(),executor:"kaggle",request_id:b.request_id||null}); if(!rd.bridge_configured){ await saveTask(env,taskId,{status:"failed",error:"UPSTREAM_AUTH_FAILED",message:"Kaggle MCP bridge is not configured",finished_at:now()}); await release(env,taskId); return error("UPSTREAM_AUTH_FAILED","Kaggle MCP bridge is not configured",503,{task_id:taskId}); } const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),15000); try{ const headers={"content-type":"application/json","accept":"application/json"}; if(env.KAGGLE_MCP_AUTH) headers.authorization=`Bearer ${env.KAGGLE_MCP_AUTH}`; const r=await fetch(env.KAGGLE_MCP_BRIDGE_URL,{method:"POST",headers,body:JSON.stringify({task_id:taskId,profile,input:b.input||{},timeout_seconds:Math.min(int(env.MAX_RUNTIME_SECONDS,3600),int(b.timeout_seconds,1800)),gpu:Boolean(b.gpu||profile==="gpu")}),signal:controller.signal}); const out=await r.json().catch(()=>null); if(!r.ok){throw Object.assign(new Error("UPSTREAM_UNAVAILABLE"),{details:redact(out),status:r.status});} const patch={status:"running",upstream:redact(out),remote_status_url:out?.status_url||null,remote_cancel_url:out?.cancel_url||null,started_at:now()}; await saveTask(env,taskId,patch); return json({ok:true,task_id:taskId,status:"running",executor:"kaggle",upstream:redact(out)},202); }catch(e){ await saveTask(env,taskId,{status:"failed",error:e.message||"UPSTREAM_UNAVAILABLE",details:redact(e.details),finished_at:now()}); await release(env,taskId); return error(e.name==="AbortError"?"TIMEOUT":"UPSTREAM_UNAVAILABLE","Kaggle dispatch failed",502,{task_id:taskId}); }finally{clearTimeout(timer);} } return error("INVALID_REQUEST","Route not found",404); }
export default {async fetch(req,env,ctx){try{return await handle(req,env,ctx)}catch(e){return error(e.message||"INTERNAL_ERROR","Request failed",e.status||500)}}};
