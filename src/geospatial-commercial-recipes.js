const MAX_ARGS_BYTES=64000;
const SAFE_ID=/^[a-z0-9_.-]{1,180}$/;
const METHODS=new Set(["spatial_feature_fusion","site_ranking","white_space"]);
const FEATURES=[
  "population_market","working_age","population_nowcast","activity_chain_demand","modelled_od_demand","modelled_destination_attraction","modelled_dwell_proxy",
  "poi_supply","poi_diversity","brand_presence","chain_brand_density","poi_turnover","commercial_supply_change",
  "building_intensity","nonresidential_built","built_land_cover","green_space","night_activity",
  "walk_accessibility","drive_accessibility","transit_accessibility","temporal_accessibility","road_traffic","observed_public_transport_activity","observed_parking_occupancy","observed_shared_bike_activity","observed_road_speed_activity","synthetic_mobility",
  "urban_growth","commercial_landuse","macro_demand","competition_pressure","hazard_risk","vacancy_risk"
];
const DEFAULT_WEIGHTS={
  population_market:0.17,working_age:0.05,population_nowcast:0.07,activity_chain_demand:0.07,modelled_od_demand:0.05,modelled_destination_attraction:0.06,modelled_dwell_proxy:0.04,
  poi_supply:0.06,poi_diversity:0.06,brand_presence:0.05,chain_brand_density:0.03,poi_turnover:0.03,commercial_supply_change:0.04,
  building_intensity:0.07,nonresidential_built:0.08,built_land_cover:0.04,green_space:0.01,night_activity:0.09,
  walk_accessibility:0.09,drive_accessibility:0.05,transit_accessibility:0.07,temporal_accessibility:0.07,road_traffic:0.04,observed_public_transport_activity:0.04,observed_parking_occupancy:0.03,observed_shared_bike_activity:0.02,observed_road_speed_activity:0.03,synthetic_mobility:0.04,
  urban_growth:0.06,commercial_landuse:0.05,macro_demand:0.10,
  competition_pressure:-0.10,hazard_risk:-0.06,vacancy_risk:-0.09
};
function fail(message,status=400,details){throw Object.assign(new Error(message),{status,details})}
function methodOf(modelId){const s=String(modelId||"").trim().toLowerCase();if(!SAFE_ID.test(s))fail("INVALID_MODEL_ID");return s.split(".").at(-1)}
export function geospatialCommercialRecipeFor(modelId){const method=methodOf(modelId);return METHODS.has(method)?{model_id:String(modelId),method,recipe:"geospatial_commercial_feature_fusion"}:null}
export function geospatialCommercialRecipeMeta(){return{
  provider:"kaggle-geospatial-commercial-domain",arbitrary_code:false,enable_internet:false,max_args_bytes:MAX_ARGS_BYTES,
  methods:[...METHODS].sort(),recipe_families:["geospatial_commercial_feature_fusion"],observed_lbs:false,raw_bulk_data:false,
  spatial_unit_policy:"arbitrary-bounded-units-h3-polygons-isochrones-or-administrative-areas",
  feature_scale:"normalized-0-to-100-with-provenance-receipts",features:FEATURES,
  evidence_policy:{synthetic_mobility_is_observed_lbs:false,public_aggregate_mobility_is_phone_lbs:false,modelled_od_is_observed_od:false,modelled_dwell_is_observed_dwell:false,air_quality_in_commercial_core:false,search_engines_are_feature_sources:false},
  reused_shared_models:{site_selection:["facility_location","commercial_spatial_fusion"],logistics:["facility_location","vrp"],change_detection:["change_point"],causal_impact:["effect_estimation"],uncertainty:["sobol"],demand_choice:["multinomial_logit"]},
  mobility_digital_twin:{activity_demand:"ActivitySim-compatible normalized features",network_assignment:"AequilibraE/SUMO-compatible normalized features",temporal_accessibility:"R5/OpenTripPlanner-compatible normalized features",population_nowcast:"POPCORN/WorldPop/GHSL-compatible normalized features",public_observation_calibration:true}
}}
export function buildGeospatialCommercialRecipeScript(taskId,modelId,args={}){
  const spec=geospatialCommercialRecipeFor(modelId);if(!spec)fail("MODEL_RECIPE_NOT_IMPLEMENTED",501,{model_id:modelId});
  if(!args||typeof args!=="object"||Array.isArray(args))fail("INVALID_MODEL_ARGS");
  const raw=JSON.stringify(args),bytes=new TextEncoder().encode(raw).length;if(bytes>MAX_ARGS_BYTES)fail("MODEL_ARGS_TOO_LARGE",413,{max_bytes:MAX_ARGS_BYTES});
  const units=Array.isArray(args.units)?args.units:[];if(units.length<1||units.length>200)fail("INVALID_SPATIAL_UNITS",400,{min:1,max:200});
  const receipts=Array.isArray(args.source_receipts)?args.source_receipts:[];if(receipts.length<2||receipts.length>32)fail("SOURCE_RECEIPTS_REQUIRED",400,{min:2,max:32});
  const payload=JSON.stringify({task_id:String(taskId),model_id:String(modelId),method:spec.method,args,allowed_features:FEATURES,default_weights:DEFAULT_WEIGHTS});
  return `import json,math,time\nt0=time.time(); cfg=json.loads(${JSON.stringify(payload)}); a=cfg["args"]; allowed=set(cfg["allowed_features"]); default_weights=cfg["default_weights"]\n\ndef finite(v,name):\n    try: x=float(v)\n    except Exception: raise ValueError("INVALID_"+name.upper())\n    if not math.isfinite(x): raise ValueError("INVALID_"+name.upper())\n    return x\ndef clamp(x,lo=0.0,hi=100.0): return max(lo,min(hi,x))\ndef mean(vals): return sum(vals)/len(vals) if vals else 0.0\n\nreceipts=a.get("source_receipts",[])\nif not isinstance(receipts,list) or len(receipts)<2 or len(receipts)>32: raise ValueError("SOURCE_RECEIPTS_REQUIRED")\nfor r in receipts:\n    if not isinstance(r,dict): raise ValueError("INVALID_SOURCE_RECEIPT")\n    d=str(r.get("digest_sha256") or r.get("result_digest") or "").lower()\n    if len(d)!=64 or any(c not in "0123456789abcdef" for c in d): raise ValueError("INVALID_SOURCE_DIGEST")\n    if not str(r.get("source") or "").strip(): raise ValueError("INVALID_SOURCE_RECEIPT")\n\ncustom=a.get("weights",{})\nif custom is None: custom={}\nif not isinstance(custom,dict) or any(k not in allowed for k in custom): raise ValueError("INVALID_WEIGHTS")\nweights=dict(default_weights)\nfor k,v in custom.items():\n    x=finite(v,"weight")\n    if abs(x)>10: raise ValueError("INVALID_WEIGHT")\n    weights[k]=x\n\nunits=a.get("units",[])\nif not isinstance(units,list) or len(units)<1 or len(units)>200: raise ValueError("INVALID_SPATIAL_UNITS")\nparsed=[]\nfor idx,u in enumerate(units):\n    if not isinstance(u,dict): raise ValueError("INVALID_SPATIAL_UNIT")\n    uid=str(u.get("id") or "").strip()\n    if not uid or len(uid)>160: raise ValueError("INVALID_SPATIAL_UNIT_ID")\n    f=u.get("features",{})\n    if not isinstance(f,dict) or len(f)>len(allowed): raise ValueError("INVALID_FEATURES")\n    unknown=set(f)-allowed\n    if unknown: raise ValueError("UNKNOWN_FEATURE")\n    vals={}\n    for k,v in f.items(): vals[k]=clamp(finite(v,k))\n    present=[k for k in vals if abs(float(weights.get(k,0)))>0]\n    denom=sum(abs(float(weights[k])) for k in present) or 1.0\n    signed=sum(vals[k]*float(weights[k]) for k in present)/denom\n    positive=[k for k in present if float(weights[k])>0]; neg=[k for k in present if float(weights[k])<0]\n    posden=sum(float(weights[k]) for k in positive) or 1.0\n    opportunity=sum(vals[k]*float(weights[k]) for k in positive)/posden\n    riskden=sum(abs(float(weights[k])) for k in neg) or 1.0\n    penalty=sum(vals[k]*abs(float(weights[k])) for k in neg)/riskden if neg else 0.0\n    site=clamp(opportunity-0.28*penalty)\n    demand=mean([vals[k] for k in ["population_market","working_age","population_nowcast","night_activity","synthetic_mobility","activity_chain_demand","modelled_od_demand","modelled_destination_attraction","modelled_dwell_proxy","macro_demand"] if k in vals])\n    access=mean([vals[k] for k in ["walk_accessibility","drive_accessibility","transit_accessibility","temporal_accessibility","road_traffic","observed_public_transport_activity","observed_parking_occupancy","observed_shared_bike_activity","observed_road_speed_activity"] if k in vals])\n    growth=mean([vals[k] for k in ["urban_growth","building_intensity","nonresidential_built","built_land_cover","commercial_landuse","poi_turnover","commercial_supply_change"] if k in vals])\n    supply=mean([vals[k] for k in ["poi_supply","brand_presence","chain_brand_density","commercial_supply_change"] if k in vals])\n    comp=vals.get("competition_pressure",0.0); risk=vals.get("hazard_risk",0.0); vacancy=vals.get("vacancy_risk",0.0)\n    white=clamp(0.42*demand+0.24*access+0.18*growth+0.08*vals.get("poi_diversity",0.0)+0.03*vals.get("green_space",0.0)-0.20*supply-0.28*comp-0.12*risk-0.16*vacancy)\n    coverage=len(vals)/len(allowed)\n    parsed.append({"id":uid,"features":vals,"feature_count":len(vals),"coverage_ratio":coverage,"demand_score":clamp(demand),"access_score":clamp(access),"growth_score":clamp(growth),"supply_score":clamp(supply),"opportunity_score":clamp(opportunity),"penalty_score":clamp(penalty),"site_score":site,"white_space_score":white,"signed_feature_score":clamp(50.0+signed/2.0)})\n\nmethod=cfg["method"]\nmetric="site_score" if method in ["spatial_feature_fusion","site_ranking"] else "white_space_score"\nranked=sorted(parsed,key=lambda x:(-x[metric],x["id"]))\nfor i,x in enumerate(ranked,1): x["rank"]=i\nsource_names=sorted(set(str(r.get("source"))[:80] for r in receipts))\nfeature_union=sorted(set(k for x in parsed for k in x["features"]))\nconfidence="high" if len(feature_union)>=22 and len(source_names)>=8 else "medium" if len(feature_union)>=14 and len(source_names)>=5 else "low"\nout={"ok":True,"task_id":cfg["task_id"],"model_id":cfg["model_id"],"recipe":"geospatial_commercial_feature_fusion","method":method,"observed_lbs":False,"real_footfall":False,"network_used":False,"raw_bulk_data":False,"metric":metric,"units":ranked,"unit_count":len(ranked),"source_count":len(receipts),"source_families":source_names,"feature_union":feature_union,"confidence":confidence,"evidence_boundaries":{"synthetic_mobility_observed_lbs":False,"public_aggregate_mobility_is_phone_lbs":False,"modelled_od_is_observed_od":False,"modelled_dwell_is_observed_dwell":False,"air_quality_in_commercial_core":False,"search_engines_are_feature_sources":False},"limitations":["normalized-feature-model-requires-source-specific-calibration","public-aggregate-mobility-calibrates-models-but-is-not-device-level-LBS","modelled-OD-footfall-and-dwell-remain-modelled-or-proxy","proxy-not-observed-phone-footfall","search-engines-are-discovery-only-not-spatial-feature-sources","no-private-payment-card-spend","no-observed-dwell-time-or-mobile-origin-destination","no-cross-mall-observed-audience-overlap","do-not-claim-baidu-huiyan-or-tencent-location-ground-truth-equivalence"],"elapsed_s":time.time()-t0}\nopen('/kaggle/working/three-center-result.json','w').write(json.dumps(out,ensure_ascii=False,sort_keys=True))\nprint('THREE_CENTER_RESULT:'+json.dumps(out,ensure_ascii=False,sort_keys=True))`;
}
